content = """# Отчёт по домашнему заданию: Generics, Коллекции

## Постановка задачи
Домашнее задание по теме «Generics, Коллекции» включает три задачи:
1. Реализовать метод, который принимает ArrayList<T> и возвращает набор уникальных элементов.
2. Реализовать метод проверки, является ли строка анаграммой другой строки.
3. Создать класс PowerfulSet с методами для операций над множествами: пересечение, объединение, относительное дополнение.

## Задача 1 (Task1)
**Вариант 1 (классический):** используем LinkedHashSet, чтобы убрать дубликаты и сохранить порядок.  
**Вариант 2 (Stream):** применяем distinct() и собираем элементы в LinkedHashSet.

**Результат в консоли:**
сходный список: [a, b, a, в, б, a, в]
Уникальные (V1, классический): [a, b, в, б]
Уникальные (V2, Stream): [a, b, в, б]


## Задача 2 (Task2)
**Вариант 1 (сортировка):** нормализуем строки, сортируем символы и сравниваем массивы.  
**Вариант 2 (Map-счётчик):** считаем количество вхождений символов и сверяем частоты.

**Результат в консоли (пример Бейсбол–бобслей):**


## Задача 3 (Task3)
**Пересечение:**
- V1 — retainAll
- V2 — filter(set2::contains)

**Объединение:**
- V1 — addAll
- V2 — Stream.concat

**Относительное дополнение:**
- V1 — removeAll
- V2 — фильтр !set2.contains

**Результат в консоли:**
set1 = [1, 2, 3]
set2 = [0, 1, 2, 4]
Пересечение (V1): [1, 2]
Пересечение (V2): [1, 2]
Объединение (V1): [0, 1, 2, 3, 4]
Объединение (V2): [0, 1, 2, 3, 4]
Отн. дополнение (V1): [3]
Отн. дополнение (V2): [3]


## Вывод
Все задачи выполнены в двух стилях: классическом (через базовые методы коллекций) и современном (через Stream API).  
Оба подхода дают одинаковый результат, но второй вариант короче и лучше иллюстрирует тему «Generics, Коллекции + лямбды/стримы».  
Код протестирован в IntelliJ IDEA, результаты соответствуют условию задания.
"""

path = "/mnt/data/readme.md"
with open(path, "w", encoding="utf-8") as f:
f.write(content)

path


